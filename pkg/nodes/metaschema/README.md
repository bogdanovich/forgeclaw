# JSON Schema Meta-Schema

These files are the JSON Schema draft 2020-12 meta-schema distributed with
`github.com/google/jsonschema-go` v0.4.3. They are embedded so node capability
schemas can be validated without network access.
